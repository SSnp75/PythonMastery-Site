---
title: Cheat Sheets
description: Quick reference cards for Python topics
---

# 📋 Cheat Sheets

Quick-reference cards — print them, bookmark them, keep them open while coding.

---

## Core Python

| Topic | Key commands |
|---|---|
| **Strings** | `f""`, `.split()`, `.join()`, `.strip()`, `[:]` slicing |
| **Lists** | `.append()`, `.pop()`, `.sort()`, `[x for x in ...]` |
| **Dicts** | `.get()`, `.items()`, `.keys()`, `{k: v for ...}` |
| **Sets** | `\|`, `&`, `-`, `^`, `.add()`, `.discard()` |
| **Files** | `with open() as f:`, `f.read()`, `f.write()` |

---

## Data Science

| Library | Key imports |
|---|---|
| NumPy | `np.array`, `np.zeros`, `np.arange`, `.reshape()` |
| Pandas | `pd.DataFrame`, `.groupby()`, `.merge()`, `.apply()` |
| Matplotlib | `plt.plot()`, `plt.subplots()`, `plt.savefig()` |

---

## Web

| Framework | Key patterns |
|---|---|
| FastAPI | `@app.get()`, `BaseModel`, `Depends()`, `async def` |
| Flask | `@app.route()`, `request.json`, `jsonify()` |
| SQLAlchemy | `Session`, `mapped_column`, `.query()`, `.filter()` |

---

<div class="pm-coming-soon">
<h3>📝 More cheat sheets coming</h3>
<p>Regex, asyncio, testing, git commands, Docker</p>
</div>
